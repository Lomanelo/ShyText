# Keep EAS upload lean (store screenshots + local tooling are not needed in the binary)
store/
.cursor/
.agents/
.impeccable/
.expo/
*.log
agent-transcripts/
# Marketing media is published from shytext.com/public/marketing — never ship in the binary
app/Videos/
